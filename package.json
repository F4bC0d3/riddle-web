{
  "name": "riddle-web",
  "version": "1.0.0",
  "description": "Tom Riddle's diary — web port. Write on screen, the diary answers in handwritten ink.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "invite": "node scripts/invite-manager.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
