name = "tomriddle"
main = "src/worker.js"
compatibility_date = "2024-09-23"

# Default backend needs at least one secret or it falls through to BYOK:
#   wrangler secret put NVIDIA_API_KEY     (primary)
#   wrangler secret put OPENROUTER_API_KEY (fallback)
